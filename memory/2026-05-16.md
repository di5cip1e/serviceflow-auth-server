# 2026-05-16

## 01:21 UTC — Mailgun Investigation

**Problem:** Mailgun returns 403 `"Domain aginstitute.tech is not allowed to send: exposed account credentials"`

**Root Cause:** The Mailgun API key has been flagged as "exposed" by Mailgun's security system. This happens when an API key is detected in a public repo, pastebin, or other exposed location.

**What's configured:**
- MAILGUN_API_KEY: f3329607... (in secrets.json)
- MAILGUN_DOMAIN: aginstitute.tech
- MAILGUN_FROM: derekbrooks@aginstitute.tech

**Domain DNS status (all sending records valid):**
- SPF: `v=spf1 include:_spf.mail.hostinger.com include:mailgun.org ~all` ✅
- DKIM: valid ✅
- CNAME: valid ✅
- MX: points to Hostinger (not Mailgun) — this is fine for sending, only affects receiving

**Fix needed:** Regenerate the Mailgun API key:
1. Log in to https://app.mailgun.com
2. Go to Settings → API Keys
3. Generate a new "Private API Key"
4. Update `~/.openclaw/secrets.json` with the new key
5. Restart backend: `pm2 restart maikr-backend`

**Not in git history** — the key was never committed to any repo in the workspace.

## 01:14 UTC — Derek asked to tackle Mailgun issue
- Investigated and diagnosed the problem
- Waiting for Derek to regenerate the API key

## 15:05 UTC — Skills-Based Improvement Sprint

Derek asked to utilize new skills to improve maikr.pro. Here's what was done:

### 1. Analytics Integration (analytics skill)
- Created `frontend/js/analytics.js` — Umami analytics wrapper with graceful fallback
- Created `frontend/analytics-config.json` — config file (disabled by default, needs site ID)
- Added Umami script + analytics.js to: landing.html, login.html, register.html, command-center.html
- Placeholder site ID: `YOUR_SITE_ID_HERE` — Derek needs to sign up at cloud.umami.is and add their ID

### 2. Stripe Best Practices (stripe-best-practices skill)
- Added `automatic_tax: { enabled: true }` to checkout session
- Added `billing_address_collection: 'required'`
- Added `phone_number_collection: { enabled: true }`
- Added `allow_promotion_codes: true`
- Added `tax_id_collection: { enabled: true }`
- Added `customer_email` pre-fill from session
- Added `client_reference_id` from user session
- Added `subscription_data.metadata` for subscription-level metadata
- Fixed `success_url` and `cancel_url` — changed from `http://` to `https://`
- Fixed pricing fallback from `PRICING.basic` to `PRICING.value`

### 3. Data Visualization (data-visualization skill)
- Added Chart.js 4.4.0 CDN to command-center.html
- Added 3 charts section to Command Center:
  - Token Usage line chart (7-day sample data)
  - Credits Burn Rate horizontal bar chart
  - Agent Activity doughnut chart
- Charts use existing dark theme colors, responsive layout
- Placeholder data — will connect to real API in next iteration

### 4. Email System Improvements (email-automation skill)
- Updated `alerter.js` to support HTML emails (added `htmlBody` parameter)
- Updated `provisioning.js` to use HTML email templates from `emails.js`
- Created `services/onboarding-emails.js` — 4-email drip campaign:
  - Day 1: Getting Started Guide
  - Day 3: Pro Tips + Use Cases
  - Day 7: Check-in + Support
  - Day 14: Advanced Features + Upgrade Prompt
- Created `services/onboarding-scheduler.js` — daily cron to send drip emails
- Created `email_log` table in database to track sent emails
- Cron job `maikr-onboarding-emails` set for 10:00 UTC daily

### 5. SaaS Revenue Tracking (saas-revenue-tracker skill)
- Created `routes/revenue.js` — API endpoints:
  - GET /api/revenue/metrics — MRR, ARR, churn, LTV, plan breakdown
  - GET /api/revenue/customers — customer list with revenue data
  - GET /api/revenue/transactions — credit transaction history
- Created `services/revenue-snapshot.js` — daily MRR snapshot logger
- Created `revenue_snapshots` table in database
- Cron job `maikr-daily-revenue-snapshot` set for 23:59 UTC daily
- All revenue routes require API key auth

### 6. Database Changes
- Added `email_log` table (tracks onboarding email sends)
- Added `revenue_snapshots` table (daily MRR tracking)
- Both tables created via IF NOT EXISTS — safe to run on existing DB

### Backend Status
- All syntax checks passed
- Backend restarted (pm2 restart maikr-backend) — online, 20th restart
- Health endpoint: OK
- All new files verified serving correctly
