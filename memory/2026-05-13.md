# 2026-05-13 — Daily Memory

## MEMORY_DREAM_TRANSFER (04:00 UTC)
- New day: Wednesday May 13, 2026
- Status: forgeai.sbs 301 (normal)
- All systems operational

## May 12 Summary (from maikr-audit & fixes)
- Site fully restored after PM2 daemon died during gateway restart
- Backend now under PM2 supervision with process list saved
- Cron watchdog installed (every 5 min) for port 3001 monitoring
- Langfuse tracer.js bug fixed (startActiveSpan undefined → guarded access)
- Git commit: fdab182

### Technical Findings
- Langfuse v5 SDK `startActiveSpan` is `undefined` — only `startActiveObservation` exists
- tracer.js was calling `tracing.startActiveSpan.bind(tracing)` → crash on startup  
- Fix: guard with `typeof` check before bind
- Backend still starts and works without Langfuse (console.warn only)

### Model Change
- Updated agent model to: openrouter/nvidia/nemotron-3-super-120b-a12b:free
- 256K token context window (massive upgrade from previous ~200k limit)
- FREE tier - no cost
- Gateway restarted to apply

### Telegram /modular Still Broken (Known Issue)
- sessions_send → 401 error from ACP runtime when processing /modular command
- Workaround: direct Telegram API calls for inline keyboard menus
- Inline keyboard works (sent directly via Bot API) but agent session can't process callbacks
- Credit issue was NOT the problem (Derek fixed OpenRouter credits separately by adding funds)

### Ongoing Tasks
- Fix /api/route-test 404 (route not implemented in backend)
- Harden nginx with proxy_next_upstream for seamless failover
- Never restart gateway without also restarting backend
- Add proper error handling for backend startup failures

### Site Status (04:00 UTC)
- ✅ Backend: PM2 PID ?, healthy (restarted during maintenance window)
- ✅ Nginx: Serving maikr.pro correctly
- ✅ Model: openrouter/nvidia/nemotron-3-super-120b-a12b:free (256K context, FREE)
- ✅ Watchdog: Cron job active (every 5 min) for port 3001 monitoring
- ✅ Git: All changes pushed to serviceflow-auth-server

## Session Context (10:22 UTC — pre-restart)

### Active Issue: CSS Rendering on Build Steps
- **Problem**: build-step*.html files show raw CSS (.maikr-nav, .maikr-logo, etc.) as visible text at top of page instead of rendering nav
- **Root cause**: Malformed HTML — multiple duplicate font link tags, corrupted <style> tags due to sed operations
- **Status**: NOT YET FIXED — needs manual cleanup of all 4 build-step files
- **Files affected**: build-step1.html, build-step2.html, build-step3.html, build-step4.html
- **Fix needed**: Remove duplicate font links, ensure clean head section with proper <style> blocks

### Model Change
- Switched to: openrouter/deepseek/deepseek-v4-pro
- Context: 1,048,576 tokens (1M)
- Pricing: $0.435/M prompt, $0.87/M completion

### Backend Status
- Running under PM2 (PID last known: 2330409)
- Cron watchdog active (every 5 min)
- Health check: https://maikr.pro/health

### Telegram /modular Command
- Still broken: sessions_send → 401 error from ACP runtime
- Workaround: Direct Telegram Bot API calls work for inline keyboards
- Callback data format: `group:agent-builder`, `group:creative`, etc.

### Cron Watchdog
- Script: /usr/local/bin/maikr-backend-watchdog.sh
- Crontab: */5 * * * *
- Restarts backend if port 3001 is down

### Git State
- Last commit: fdab182 (Langfuse tracer.js fix)
- Unpushed: CSS changes + daily lessons review file

### Derek's Active Requests
- Fix CSS rendering on build steps (HIGH PRIORITY)
- Switch model to deepseek/deepseek-v4-pro
- Credit system was fixed (Derek added OpenRouter credits, not a code fix)

## Evening Session (16:00-23:30 UTC)

### Full Audit Results
- All systems operational: gateway, backend, nginx, SSL, all 11 pages
- PM2 was EMPTY — backend running as raw process (critical)
- Nightly optimization cron in ERROR state (SQL syntax bug)
- Model override to owl-alpha rejected (not in models registry)
- Gateway not under any supervisor

### Fixes Applied
1. **P0: Backend under PM2** — maikr-backend now supervised, auto-restarts
2. **P1: Optimization SQL fix** — analyzeUserCorrections had mixed quotes in IN clause; rewrote with LOWER() + proper escaping
3. **P1: Model owl-alpha registered** — added to agents.defaults.models registry; switched to openrouter/owl-alpha (free, 1M+ context)
4. **P2: Gateway under PM2** — openclaw-gateway now supervised

### Auth & Onboarding Design
- Derek requested auth system + onboarding flow design
- Created AUTH-DESIGN.md: session-based auth (email+password), bcrypt, express-session + connect-sqlite3
- Created TASKS.md: 8 tasks, designed for subagent execution across sessions
- Created skill: skills/task-decomposer/SKILL.md for reusable task decomposition
- 8 tasks: DB migration → middleware → auth routes → login UI → settings → checkout link → protect pages → E2E test
- Committed: feat: auth & onboarding design + task decomposer skill

### Model Status
- Active: openrouter/owl-alpha (free, 1,048,576 context)
- Config: agents.defaults.model = openrouter/owl-alpha
- Models registry: openrouter/auto, openrouter/minimax/minimax-m2.5, openrouter/owl-alpha
